spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local

---
spring:
  profiles: nonprod
  data:
    mongodb:
      port: 27017
      host: localhost1
      database: local

---
spring:
  profiles: test
